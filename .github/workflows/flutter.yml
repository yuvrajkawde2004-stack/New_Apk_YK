name: Build Flutter APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.3'

      - run: flutter pub get

      - run: flutter build apk --release

         - name: Upload APK to free server
     run: curl -T build/app/outputs/flutter-apk/app-release.apk https://bashupload.com/app-release.apk
